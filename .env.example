# Hephaestus API Keys Configuration
# Copy this file to .env and add your actual keys

## LLM Provider Configuration

# OpenAI Configuration
# Always required for embeddings (text-embedding-3-large)
OPENAI_API_KEY=sk-proj-your-key-here
USE_OPENAI=true

# Anthropic Configuration
# Note: Anthropic API may be used even without a key if you have a subscription
# configured through other means (e.g., Claude subscription, organization setup)
USE_ANTHROPIC_API=true
ANTHROPIC_API_KEY=your-key-here

# Groq Configuration
GROQ_ENABLED=true
GROQ_API_KEY=your-key-here

# OpenRouter Configuration
OPENROUTER_ENABLED=true
OPENROUTER_API_KEY=sk-or-v1-your-key-here

## Provider Enable/Disable Flags
# Set to 'false' to disable a provider entirely (won't be used even if API key is present)
# Set to 'true' to enable a provider (will use API key if provided)

# Example: Disable Groq even if key is present
# GROQ_ENABLED=false

# Example: Use Anthropic without API key (if subscription is configured)
# USE_ANTHROPIC_API=false
# ANTHROPIC_API_KEY=  # Leave empty if using subscription
